# 子集/划分不稳定 + 图谱粒度基准 — 冻结环境(2026-06-13 实测产出本结果的版本)
# Python 3.11.8 (pyenv virtualenv data-analysis), macOS Apple Silicon, CPU only
# 注意: tabpfn 要求 pandas<3; 切勿装 nilearn(会把 pandas 顶到 3.x 破坏 tabpfn)。
# 图谱重采样用 nibabel+scipy(见 src/s23), 不依赖 nilearn。
numpy==1.26.4
pandas==1.5.1
scipy==1.16.2
scikit-learn==1.7.2
xgboost==3.1.1
lightgbm==4.6.0
imbalanced-learn==0.14.1
statsmodels==0.14.5
nibabel==5.4.2
matplotlib==3.10.7
tabpfn==6.3.2
